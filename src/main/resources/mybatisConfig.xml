<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.base.BaseMapper">
    <select id="loadModeForAll" parameterType="String" resultType="Object">
        ${sql}
    </select>
    <select id="loadModeForList" parameterType="String" resultType="Object">
        ${sql}
    </select>
    <select id="loadModeForCount" parameterType="String" resultType="Object">
        ${sql}
    </select>
    <select id="loadModeForOne" parameterType="String" resultType="Object">
        ${sql}
    </select>
    <insert id="addModeForOne" parameterType="String" keyProperty="id" useGeneratedKeys="true">
        ${sql}
    </insert>
    <insert id="addModeForBatch" parameterType="Map">
        <foreach collection="list" index="index" item="item" open="" separator=";" close=";">
            ${item}
        </foreach>
    </insert>
    <delete id="delModeForOne" parameterType="String">
        ${sql}
    </delete>
    <delete id="delModeForBatch" parameterType="List">
        <foreach collection="list" index="index" item="item" open="" separator=";" close=";">
            #{item}
        </foreach>
    </delete>
    <update id="updateModeForOne" parameterType="String">
        ${sql}
    </update>
    <update id="updateModeForBatch" parameterType="List">
        <foreach collection="list" index="index" item="item" open="" separator=";" close=";">
            #{item}
        </foreach>
    </update>
</mapper>